{
  "name": "phaser-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild --bundle src/index.js --loader:.png=file --outfile=dist/index.js --watch",
    "build": "esbuild --bundle src/index.js --loader:.png=file --outfile=dist/index.js"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.17.15"
  },
  "dependencies": {
    "easystarjs": "^0.4.4"
  }
}
